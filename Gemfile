# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.2.2'

# Use faye-websocket for easily building WebSocket servers and clients in Ruby
gem 'faye-websocket'

# Use Sinatra for all your classy webdev needs
gem 'sinatra'

# Use Puma for web server
gem 'puma'

group :development, :test do
  # Use rubocop for linting and code formatting / quick fixes
  gem 'rubocop'
end
